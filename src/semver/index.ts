export interface Version { major:number;minor:number;patch:number;prerelease:string[] }
export function parseVersion(input:string):Version|null{const match=/^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-([0-9A-Za-z-]+(?:\.[0-9A-Za-z-]+)*))?(?:\+[0-9A-Za-z-]+(?:\.[0-9A-Za-z-]+)*)?$/.exec(input);return match?{major:+match[1],minor:+match[2],patch:+match[3],prerelease:match[4]?.split('.')??[]}:null}
export function compareVersion(left:string,right:string):number{const a=parseVersion(left),b=parseVersion(right);if(!a||!b)throw new TypeError('Invalid semantic version');for(const key of ['major','minor','patch'] as const)if(a[key]!==b[key])return a[key]<b[key]?-1:1;if(!a.prerelease.length||!b.prerelease.length)return a.prerelease.length===b.prerelease.length?0:a.prerelease.length?-1:1;for(let i=0;i<Math.max(a.prerelease.length,b.prerelease.length);i++){const x=a.prerelease[i],y=b.prerelease[i];if(x===undefined)return-1;if(y===undefined)return 1;if(x===y)continue;const xn=/^\d+$/.test(x),yn=/^\d+$/.test(y);if(xn&&yn)return +x<+y?-1:1;if(xn!==yn)return xn?-1:1;return x<y?-1:1}return 0}
/** Supports exact versions, comparison operators, whitespace AND, || OR, caret, and tilde ranges. */
export function satisfiesVersion(version:string,range:string):boolean{if(!parseVersion(version))return false;return range.split(/\s*\|\|\s*/).some(group=>group.trim().split(/\s+/).every(term=>{if(!term)return true;const match=/^(\^|~|>=|<=|>|<|=)?(.+)$/.exec(term)!;const operator=match[1]??'=',base=match[2],parsed=parseVersion(base);if(!parsed)return false;const cmp=compareVersion(version,base);if(operator==='=')return cmp===0;if(operator==='>')return cmp>0;if(operator==='>=')return cmp>=0;if(operator==='<')return cmp<0;if(operator==='<=')return cmp<=0;const upper=operator==='~'?`${parsed.major}.${parsed.minor+1}.0`:parsed.major>0?`${parsed.major+1}.0.0`:parsed.minor>0?`0.${parsed.minor+1}.0`:`0.0.${parsed.patch+1}`;return cmp>=0&&compareVersion(version,upper)<0}))}
